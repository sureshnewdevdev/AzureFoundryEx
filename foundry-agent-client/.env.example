# Optional overrides. The scripts already have these values baked in as defaults,
# so you can run them without setting anything. Set these only if you want to
# point the scripts at a different project/agent/model.

FOUNDRY_PROJECT_ENDPOINT=https://day1flow-resource.services.ai.azure.com/api/projects/day1flow
AGENT_NAME=KnowledgeTest

AGENT_OPENAI_BASE_URL=https://day1flow-resource.services.ai.azure.com/api/projects/day1flow/agents/KnowledgeTest/endpoint/protocols/openai
MODEL=gpt-4o-mini
